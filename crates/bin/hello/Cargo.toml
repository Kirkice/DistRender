[package]
name = "hello"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
dist-render = { path = "../../lib/dist-render" }
dist-render-simple = { path = "../../lib/dist-render-simple" }
anyhow = "1.0"
